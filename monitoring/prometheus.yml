global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'ocpp-backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['app:8080']
        labels:
          application: 'ocpp16-ws-backend'
