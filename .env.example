# Docker Compose Configuration
# Copy to .env and customize for your environment

# --- Application ---
# Leave empty to build locally, or set to pull from GHCR:
# APP_IMAGE=ghcr.io/org-tekeli-borisp/ocpp16-ws-backend:latest
# APP_IMAGE=ghcr.io/org-tekeli-borisp/ocpp16-ws-backend:latest-native
APP_IMAGE=

# Dockerfile for local builds (JVM or Native):
DOCKERFILE=src/main/docker/Dockerfile.jvm

# Ports
APP_PORT=8080
POSTGRES_PORT=5432
SFTP_PORT=2022
FTP_PORT=2021

# --- FTP Passive Mode & SFTP URL Generation ---
# Set to your LAN IP so charge points can reach FTP (passive data) and SFTP
OCPP_FTP_EXTERNAL_ADDRESS=127.0.0.1

# --- PostgreSQL ---
POSTGRES_DB=ocpp
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-in-production

# --- Monitoring (docker-compose.monitoring.yml) ---
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me-in-production
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
